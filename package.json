{
  "name": "diffly",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5000 --bind 0.0.0.0",
    "build": "node --check src/app.js && node --check src/lib/diff.js",
    "preview": "python3 -m http.server 5000 --bind 0.0.0.0",
    "test": "node --test"
  },
  "dependencies": {},
  "devDependencies": {}
}
